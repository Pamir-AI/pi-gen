--- stage2.orig/rootfs/boot/firmware/config.txt
+++ stage2/rootfs/boot/firmware/config.txt
@@ -36,8 +36,12 @@ arm_64bit=1
 # Disable compensation for displays with overscan
 disable_overscan=1
 
-# Run as fast as firmware / board allows
-arm_boost=1
+# --- Cap CPU speed to 80 % of 2.4 GHz ---
+arm_freq=1500    
+arm_freq_min=600   # let it idle as low as 600 MHz
+
+# Pi 5 / CM5 only — turn off the temporary 3 GHz turbo boost
+arm_boost=0
 
 [cm4]
 # Enable host mode on the 2711 built-in XHCI USB controller.
